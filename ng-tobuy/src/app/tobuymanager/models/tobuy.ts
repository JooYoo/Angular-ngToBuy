export class Tobuy {
    id: number;
    text: string;
    isDone: boolean;

    constructor(id: number, text:string, isDone:boolean){
        this.id = id;
        this.text= text;
        this.isDone = isDone;
    }
}